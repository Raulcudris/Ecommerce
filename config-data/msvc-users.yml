server:
  port: 8004
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    instance-id: localhost
  admin-paths:
    paths:
      - uri: "/products/**"
        method: "POST"
      - uri: "/products/{1}[1-9]+[0-9]*"
        method: "GET"
      - uri: "/products/**"
        method: "GET"
  user-paths:
    paths:
      - uri: "/products/{1}[1-9]+[0-9]*"
        method: "GET"
      - uri: "/products/**"
        method: "GET"